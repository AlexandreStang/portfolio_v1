#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pleeease-cli/bin/pleeease" "$@"
  ret=$?
else 
  node  "$basedir/../pleeease-cli/bin/pleeease" "$@"
  ret=$?
fi
exit $ret
